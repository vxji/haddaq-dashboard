# برومت لـ Claude Code: نظام مكتب الحداق للاستشارات الهندسية

انسخ النص التالي كاملاً وأعطه لـ Claude Code في بداية المشروع.

---

## السياق والهدف

ابنِ لي نظام Dashboard ويب احترافي متكامل باسم **"مكتب الحداق للاستشارات الهندسية"** لإدارة المشاريع الهندسية والعقود والمتابعة الميدانية. ابدأ بإنشاء خطة عمل (plan) مقسّمة على مراحل قبل كتابة أي كود، ثم نفّذ كل مرحلة وتحقق من عملها قبل الانتقال للتالية.

## التقنيات (Stack) الإلزامية

- **Frontend:** Next.js 15 (App Router) + TypeScript + Tailwind CSS + shadcn/ui + React Hook Form + Zod للتحقق من المدخلات
- **Backend:** Next.js API Routes / Server Actions
- **قاعدة البيانات:** Supabase (PostgreSQL) مع Row Level Security (RLS) لكل جدول حسب الصلاحيات
- **تخزين الملفات:** Supabase Storage
- **البريد/الإشعارات:** Nodemailer
- **توليد PDF:** pdf-lib
- **الرسوم البيانية:** Recharts
- **الخط الزمني:** مكتبة Timeline مناسبة لـ React (أو بناء مكون مخصص)
- **التقويم:** FullCalendar
- **الدردشة الفورية:** Socket.io (أو Supabase Realtime كبديل أبسط - اقترح الأنسب)
- **التصميم:** ألوان أساسية أزرق داكن + أحمر + أبيض، دعم كامل للغة العربية RTL، تصميم متجاوب (Mobile/Desktop)

## هيكلة قاعدة البيانات

أنشئ سكيمة Supabase (migrations SQL) للجداول التالية مع العلاقات (Foreign Keys) والفهارس المناسبة:
`Users, Roles, Permissions, Projects, Contracts, Payments, Stages, Tasks, Files, Comments, ChatMessages, Notifications, CalendarEvents, ActivityLogs`

اجعل كل عملية إضافة/تعديل/حذف تُسجَّل تلقائياً في `ActivityLogs` (بدون إمكانية حذف السجل من واجهة المستخدم).

## نظام المستخدمين والصلاحيات

- صفحة تسجيل دخول (اسم مستخدم + كلمة مرور)، إنشاء حساب جديد، تغيير كلمة المرور، نسيان كلمة المرور (عبر البريد).
- إنشاء مستخدمين جدد متاح **للمدير فقط**.
- دورين أساسيين قابلين للتوسعة لاحقاً عبر جدول Permissions:
  - **مدير النظام:** إنشاء/حذف مستخدمين، تعديل الصلاحيات، رؤية كل المشاريع، رؤية سجل التعديلات الكامل.
  - **موظف:** تعديل المشاريع المسندة له، إضافة ملاحظات، رفع ملفات، استخدام الدردشة.
- استخدم Supabase Auth + RLS فعلياً لفرض هذه الصلاحيات على مستوى قاعدة البيانات وليس فقط في الواجهة.

## الصفحات المطلوبة

1. **تسجيل الدخول** (login / إنشاء حساب / نسيان كلمة المرور)
2. **Dashboard رئيسية:** عدد المشاريع (الحالية/المنتهية/قيد التنفيذ)، العقود غير الموقّعة، المبالغ المستحقة، آخر الأنشطة، إشعارات المهام، رسوم بيانية (Recharts).
3. **المشاريع:** جدول يحتوي كل الحقول التالية: رقم المشروع، اسم المشروع، اسم المالك، رقم الهوية، رقم الجوال، العنوان، تاريخ العقد، حالة توقيع العقد، إجمالي قيمة العقد، المبلغ المدفوع، المبلغ المتبقي، حالة المشروع، المهندس المسؤول، ملاحظات، مرفقات، عقد PDF. مع تعديل/حذف/طباعة العقد.
4. **بحث متقدم:** بحث فوري برقم الهوية، اسم المالك، رقم المشروع، رقم الجوال.
5. **تفاصيل المشروع:** تبويبات (المعلومات الأساسية / العقد / المرفقات) — رفع ودعم ملفات PDF, Word, Excel, صور, AutoCAD, ZIP.
6. **مراحل المشروع (Timeline):** 5 مراحل ثابتة بألوان محددة (معماري=أزرق، إنشائي=أحمر، كهربائي=برتقالي، ميكانيكي=أخضر، اعتماد وتسليم=بنفسجي)، لكل مرحلة: تواريخ، نسبة إنجاز، حالة، وصف، رفع ملفات، ملاحظات، شريط تقدم، وعلامة ✓ عند الاكتمال.
7. **التقويم:** عرض شهري/أسبوعي لمواعيد التسليم، الاجتماعات، المهام القادمة، المشاريع المتأخرة.
8. **التنبيهات:** إشعار داخل النظام + بريد إلكتروني عند إكمال مرحلة/إضافة ملاحظة/تعديل مشروع/رفع ملف، وتنبيهات تلقائية قبل التسليم بـ 7 أيام و3 أيام ويوم واحد.
9. **الملاحظات:** لكل ملاحظة اسم المستخدم + التاريخ/الوقت + النص + إمكانية الرد.
10. **الدردشة الداخلية:** غرفة محادثة لكل مشروع، إرسال نصوص/صور/PDF/مستندات، وإظهار من قرأ الرسالة.
11. **سجل النشاط (Activity Log):** عرض كل العمليات (من نفّذ، نوع العملية، التاريخ/الوقت) بدون حذف.
12. **إدارة العقود:** إنشاء عقد تلقائياً من بيانات المالك والمبالغ، توليد PDF احترافي، طباعة/تحميل/إرسال بالبريد.
13. **المستخدمون:** إضافة مستخدم (الاسم، البريد، الجوال، المسمى الوظيفي، الصلاحيات، كلمة المرور، الحالة نشط/معطل).

## متطلبات تصميم إضافية

- دعم RTL كامل في كل الصفحات والمكونات (بما فيها الجداول والتقويم والـ Timeline).
- استخدام shadcn/ui كقاعدة، مع تخصيص الألوان (أزرق داكن، أحمر، أبيض) عبر متغيرات Tailwind/CSS.
- تصميم متجاوب بالكامل للجوال.

## خطوات العمل المطلوبة من Claude Code

1. اقترح هيكلة المجلدات (folder structure) للمشروع كاملة قبل البدء.
2. أنشئ ملفات الـ SQL migrations لقاعدة بيانات Supabase أولاً.
3. ابنِ نظام المصادقة والصلاحيات (Auth + RLS).
4. ابنِ الصفحات تباعاً حسب الترتيب أعلاه، صفحة كاملة وظيفية قبل الانتقال للتالية.
5. بعد كل مرحلة كبيرة، اعرض ملخصاً لما تم إنجازه وما تبقى.
6. اجعل المشروع قابلاً للنشر مباشرة على Vercel (مع وجود `.env.example` يوضح كل متغيرات البيئة المطلوبة لـ Supabase وNodemailer وغيرها).
7. لا تستخدم بيانات وهمية ثابتة (mock) في الإنتاج النهائي؛ اربط كل شيء بقاعدة البيانات الفعلية.

ابدأ الآن بخطة العمل التفصيلية، ثم انتظر موافقتي قبل توليد الكود الفعلي.
